import 'package:flutter/material.dart';

class SageScreen extends StatelessWidget {
  const SageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Sage')),
      body: const Center(
        child: Text('Sage AI Tab — Phase 3 placeholder'),
      ),
    );
  }
}
