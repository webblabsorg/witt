name: witt_ai
description: AI provider routing and client layer for Witt (Groq, OpenAI, Claude).
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  witt_core:
    path: ../witt_core
  witt_api:
    path: ../witt_api
  dio: ^5.8.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
